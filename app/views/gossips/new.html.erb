<div class="contener text-center">
</br></br>
  <h1 class="display-5">On veut tout savoir !</h1>
  </br>
</div>

<div class="contener text-center" style="margin: 10px">
  <% if flash[:fail] %> 
    <div class="alert alert-danger" role="alert">
      Une erreur s'est produite!
    </div>
  <% end %>
</div>

<% if !@gossip.nil? && @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
   
<%= form_tag "/gossips", method: "post" do %>

  <div class="form-group">
    <label for="exampleFormControlTextarea1">Titre</label>
    <input type="text" name="title"  placeholder="Titre" required class="form-control" rows="1"/>
  </div>      

  <div class="contener">
    <div class="form-group">
      <label for="exampleFormControlTextarea1">Contenu</label>
      <textarea type="text" name="content"  placeholder="Potin" class="form-control" rows="4"></textarea>
    </div>
  </div>

  <div class="form-group row">
    <div class="col-sm-10">
      <input type="submit" value="Ajouter le potin" class="btn btn-primary"/>
    </div>
  </div>


<% end %>