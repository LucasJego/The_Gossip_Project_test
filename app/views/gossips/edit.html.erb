<div class="contener text-center">
  </br></br>
  <h1>Vous êtes sur la page d'édition du potin nommé "<%= @gossip.title %>".</h1>
  </br>


  </br></br>
  <h2>Son auteur, <%= @gossip.user.first_name %> <%= @gossip.user.last_name %>, l'a enregistré le <%= @gossip.created_at.strftime("%d/%m/%Y") %>.</h2>
  <% if @gossip.created_at  != @gossip.updated_at %>
      <h4>
        <%= @gossip.updated_at.strftime("Dernière mise à jour le %d/%m/%Y") %> <%= @gossip.updated_at.strftime("à %H:%M")  %>
      </h4>
    <% end %>    
  </br>
</div>

<div class="contener text-center" style="margin: 10px">
  <% if flash[:fail] %> 
    <div class="alert alert-danger" role="alert">
      Une erreur s'est produite!
    </div>
  <% end %>
</div>

<% if !@gossip.nil? && @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à mettre à jour le potin pour les raisons suivantes :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
   
<%= form_tag gossip_path, method: "put" do %>

  <div class="form-group">
    <label for="exampleFormControlTextarea1">Titre</label>
    <input type="text" name="title"  placeholder= <%= @gossip.title %> required class="form-control" rows="1"/>
  </div>      

  <div class="contener text-left">
    <div class="form-group">
      <label for="exampleFormControlTextarea1">Contenu</label>
      <textarea type="text" name="content" class="form-control" rows="4" >
        <%= @gossip.content %>
      </textarea>
    </div>
  </div>

  <div class="form-group row">
    <div class="col-sm-10">
      <input type="submit" value="Mettre à jour" class="btn btn-primary"/>
    </div>
  </div>

<% end %>

